# Rust
/target
**/*.rs.bk
*.pdb

# Harness runtime artifacts
*.sqlite
*.sqlite-journal
*.db
/exports
*.jsonl
# Per-project harness state (chat attachments etc.) written when the harness
# runs against this repo. Versioned via Oxen, not git — EXCEPT project skills,
# which are deliberately committed so everyone who opens the repo gets them.
.oxen-harness/*
!.oxen-harness/skills

# Secrets / local config
.env
.env.*
!.env.example

# Tauri (added in a later phase)
/app/target
/app/dist
/app/node_modules

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
